<% if publicImage.new_record? %>
  window.BikeIndexAlerts.add("error", "Failed to upload publicImage: #{j publicImage.errors.full_messages.join(', ').html_safe}");
<% else %>
 $("#publicImages").append("<%= j(render partial: '/publicImages/revised_publicImage', locals: { image: publicImage }) %>");
<% end %>
