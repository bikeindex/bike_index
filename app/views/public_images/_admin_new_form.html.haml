= form_for PublicImage.new do |f|
  - if imageable_type == 'Blog'
    = hidden_field_tag :blog_id, @blog.id
  - elsif imageable_type == 'Organization'
    = hidden_field_tag :organization_id, @organization.id
  - elsif imageable_type == 'MailSnippet'
    = hidden_field_tag :mail_snippet_id, @mail_snippet.id
  .input-group
    .custom-file
      = f.label :image, "Choose file", class: "custom-file-label"
      = f.file_field :image, multiple: true, name: "public_image[image]", class: "custom-file-input"
  %p
    Drag and drop files here to or use the
    %em Upload Photos
    button.
