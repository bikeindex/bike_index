-# Rendering errors requires an `obj`
- name ||=  obj.class.name.titleize

.alert.alert-danger
  %h4
    - errors_count = pluralize(obj.errors.count, translate_with_args(".error"))
    = translate_with_args(".errors_prevented_this_from_being_saved", errors_count: errors_count, object_name: name)
  %ul
    - obj.errors.full_messages.each do |message|
      %li
        = message
