<% status_word = @registration_sequence.active? ? "Current" : @registration_sequence.status.capitalize %>

<h1 class="uncap tw:mb-6 tw:text-2xl tw:font-semibold">
  Previewing <strong><%= status_word %></strong> registration sequence
</h1>

<%#
  Full-bleed on mobile so the gray runs edge to edge, past the org page's padding. pt
  cancels the shell's negative top margin (padding doesn't collapse) so the title clears it.
%>

<div class="tw:mx-[calc(50%_-_50vw)] tw:pt-9 tw:min-[992px]:pt-15 tw:sm:mx-0">
  <%= render Org::RegistrationSequence::Preview::Component.new(registration_sequence: @registration_sequence, index: @preview_index) %>
</div>

<%# Below the gray. mt-24 clears the flow shell's -mb-18 pull-up. %>
<div class="tw:mt-24">
  <%= render UI::Pagination::Component.new(pagy: @preview_pagy,
    page_params: {organization_id: current_organization.to_param, id: @registration_sequence.id}) %>
</div>
