#requestBikeDelete.modal.hide.fade{ data: {url: ENV['BASE_URL'] + send_request_api_v1_users_path } }
  .modal-header
    %button{ type: "button", class: "close", :data => { dismiss: "modal" }, :aria => {hidden: "true"} }
      &times;
    %h3
      You sure you want to delete this #{@bike.type}?
  .modal-body.form.form-horizontal
    #request-delete-error.alert.alert-error
      %a.close{"data-dismiss" => "alert"} ×
      %h4
        We want to know why!
      %p
        Please fill in the reason for the bike deletion
    = form_tag  do |t|
      = hidden_field_tag :bike_delete_bike_id, @bike.id
      .input-group
        .control-group
          %label.control-label{ for: :bike_delete_reason }
            Reason for deletion
          .controls
            = text_area_tag :bike_delete_reason
      .input-group
        .controls.actions.padded
          %a#request-bike-delete.button-red{ href: '#' }
            Delete bike
          