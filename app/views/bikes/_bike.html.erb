<%# somewhat gross hack to make it possible to show serial_display for the current user %>
<% skip_cache ||= false %>
<% current_event_record ||= nil %>

<%= render(SearchResults::BikeBox::Component.new(bike:, event_record: current_event_record, skip_cache:, current_user:)) %>
