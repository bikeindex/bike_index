= form_tag oauth_application_url(application), method: :delete do
  %input{:name => "_method", :type => "hidden", :value => "delete"}/
  = submit_tag 'Destroy', onclick: "return confirm('Are you sure?')", class: 'btn binxbtn-danger'
