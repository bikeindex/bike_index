= render 'shared/header'
%header.global-header#header
  = render 'shared/header_nav'

%section.content
  .receptacle
    .inner-recep

      = yield

- cache 'footer_old' do 
  = render 'shared/footer'