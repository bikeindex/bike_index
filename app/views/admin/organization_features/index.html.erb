<% nav_header_list_items = capture do %>
  <li class="nav-item">
    <%= link_to "New Feature", new_admin_organization_feature_url, class: "nav-link" %>
  </li>
<% end %>

<%= render(Admin::IndexSkeleton::Component.new(
  sortable_search_params:,
  render_chart: @render_chart,
  render_deleted: @render_deleted,
  pagy: @pagy,
  per_page: @per_page,
  time_range: @time_range,
  period: @period,
  start_time: @start_time,
  end_time: @end_time,
  time_range_column: @time_range_column,
  user_subject: @user_subject,
  bike: @bike,
  marketplace_listing: @marketplace_listing,
  primary_activity: @primary_activity,
  current_organization:,
  params:,
  collection: @organization_features,
  viewing: "Organization Features",
  skip_charting: true,
  nav_header_list_items:,
)) %>
