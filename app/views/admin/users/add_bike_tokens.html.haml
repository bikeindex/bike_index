= link_to 'Back', admin_users_url, class: 'button-green'

%h1
  = "#{params[:count]} Tickets added for #{@user.name}"

%h3
  = "The user now has #{@user.bike_tokens.available.count} tickets available"
