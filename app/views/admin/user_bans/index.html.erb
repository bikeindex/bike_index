<div class="admin-subnav">
  <div class="col-md-6">
    <h1>User Bans</h1>
  </div>

  <div class="col-md-6">
    <ul>
      <li class="nav-item">
        <% if viewing_deleted? %>
          <%= link_to "Active", admin_user_bans_path(sortable_search_params.except(:deleted)), class: "nav-link" %>
        <% else %>
          <%= link_to "Deleted", admin_user_bans_path(sortable_search_params.merge(deleted: true)), class: "nav-link" %>
        <% end %>
      </li>
    </ul>
  </div>
</div>

<%= render partial: "/shared/admin/index_skeleton", locals: {chart_collection: @render_chart && matching_user_bans} %>
