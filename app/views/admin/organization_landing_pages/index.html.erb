<% table_view = render(Pages::Admin::Organizations::LandingPagesTable::Component.new(collection: @collection,
  sort_state:,
  render_sortable: true, render_search: params[:organization_id].blank?)) %>

<%= render(Pages::Admin::IndexSkeleton::Component.new(
  index: admin_index_state,
  collection: @collection,
  viewing: controller_name.humanize,
  table_view:,
  chart_collection: @render_chart && matching_organization_landing_pages,
)) %>
