%h1
  edit #{@ctype.name} component type


= render partial: "form"

= render(UI::ButtonLink::Component.new(text: "Delete component type", href: admin_ctype_url(@ctype), color: :error, html_class: "float-right", method: :delete, confirm: "Are you sure?"))
