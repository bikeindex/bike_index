%h1
  edit #{@ctype.name} component type


= render partial: "form"

= render(UI::ButtonLink::Component.new(text: "Delete component type", href: admin_ctype_url(@ctype), method: :delete, color: :error, confirm: "Are you sure?", form: {class: "float-right"}))
