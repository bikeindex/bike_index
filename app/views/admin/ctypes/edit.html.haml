%h1
  edit #{@ctype.name} component type


= render partial: "form"

= render(UI::ButtonLink::Component.new(text: "Delete component type", href: admin_ctype_url(@ctype), color: :error, confirm: "Are you sure?", html_class: "float-right", data: {turbo: true, turbo_method: :delete}))
