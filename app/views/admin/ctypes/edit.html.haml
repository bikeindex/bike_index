%h1
  edit #{@ctype.name} component type


= render partial: "form"

= link_to "Delete component type", admin_ctype_url(@ctype), method: :delete, confirm: "Are you sure?", class: "btn btn-danger float-right"
