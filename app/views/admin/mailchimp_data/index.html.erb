<%# The trigger and the data column are separate skeleton fragments, so the controller wraps the render %>
<div data-controller="ui--collapse">
  <%= render(Pages::Admin::IndexSkeleton::Component.new(
    index: admin_index_state,
    collection: @mailchimp_data,
    viewing: "Mailchimp Data",
    nav_header_list_items:,
    admin_search_form:,
    chart_collection: @render_chart && matching_mailchimp_data,
  )) %>
</div>
