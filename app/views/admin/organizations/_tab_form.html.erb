<% tab ||= nil %>
<% submit_text ||= "Update" %>

<%= form_for [:admin, @organization], html: {data: {controller: "admin--organization-form"}} do |form_builder| %>
  <%= hidden_field_tag :tab, tab if tab.present? %>

  <%= yield form_builder %>

  <div class="mt-4">
    <%= form_builder.submit submit_text, class: "btn-lg btn-success float-right" %>
  </div>
<% end %>
