- if @new_user = true
  = t(".hello_welcome_to_bike_index")
- else
  = t(".hello")

- if @sender.present?
  = t(".you_are_a_member", sender_name: @sender.display_name, org_name: @organization.name)

= t(".sign_up_or_sign_in", org_name: @organization.name)
