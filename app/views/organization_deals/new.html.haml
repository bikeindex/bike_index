%h1
  Claim your free registration from #{@organization.name}!
= form_for [@organizationDeal], html: { class: "form-horizontal padded" } do |f|
  - if @organizationDeal.errors.any?
    #alert-block
      .alert.alert-error
        %h4
          Please fix the following 
          = pluralize(@organizationDeal.errors.count, "error")
        %ul
          - @organizationDeal.errors.full_messages.each do |msg|
            %li
              = msg
  = f.hidden_field :organization_id, value: @organization.id
  = f.hidden_field :deal_name, value: @name

  .control-group
    = f.label :email, "Email", class: "control-label"
    .controls
      = f.email_field :email, :required =>true
  
  .control-group.actions.padded
    = f.submit "Claim registration", class: 'button-green'
