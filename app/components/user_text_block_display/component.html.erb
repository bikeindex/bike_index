<%# NOTE: tag open and close needs to be on one line, or the text has whitespace at the beginning %>
<div
  class="
    <% @overflow_class %> tw:whitespace-pre-wrap
    <%= @additional_classes %>
  "
><%= @text.strip %></div>
