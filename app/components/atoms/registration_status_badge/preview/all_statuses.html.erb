<ul class="tw:flex tw:flex-col tw:items-start tw:gap-3">
  <% badges.each do |badge| %>
    <li>
      <%= render(Atoms::RegistrationStatusBadge::Component.new(**badge)) %>
    </li>
  <% end %>
</ul>
