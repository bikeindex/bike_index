<%#
  The consumer page pulls itself up under the header band the application layout puts
  above it. Lookbook renders the component on its own, so without this the top of the
  page is dragged off the top of the frame.
%>

<div class="<%= "tw:min-[992px]:pt-[44px]" if offset_header %>">
  <%= render(component) %>
</div>
