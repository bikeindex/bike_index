<%#
  The consumer page pulls itself up under the layout's header band, and Lookbook renders
  it alone — without this the top of the page is dragged off the frame
%>

<div class="<%= "tw:min-[992px]:pt-[44px]" if offset_header %>">
  <%= render(component) %>
</div>
