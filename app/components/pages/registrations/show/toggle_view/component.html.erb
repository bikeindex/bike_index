<%= render(UI::Alerts::Base::Component.new(kind: :purple)) do %>
  <div class="tw:flex tw:flex-wrap tw:items-center tw:justify-between tw:gap-3">
    <% if @show_legacy %>
      <span class="tw:font-semibold">
        <%= translation(".try_new_view", bike_type: @bike.type) %>
      </span>
      <%= render(UI::ButtonLink::Component.new(text: translation(".switch_to_new"), href: toggle_legacy_view_registration_path(@bike), method: :post, color: :secondary, size: :sm)) %>
    <% else %>
      <div class="tw:font-semibold">
        <%= translation(".viewing_in_legacy") %>
        <%= render(UI::ButtonLink::Component.new(text: translation(".switch_to_legacy_default"), href: toggle_legacy_view_registration_path(@bike), method: :post, color: :link, form: {class: "tw:inline"}, html_class: "tw:font-semibold")) %>
      </div>
      <%= render(UI::ButtonLink::Component.new(text: translation(".view_in_new"), href: registration_path(@bike), color: :secondary, size: :sm)) %>
    <% end %>
  </div>
<% end %>
