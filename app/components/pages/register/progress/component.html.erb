<div class="tw:mb-5 tw:flex tw:items-center tw:gap-1.5">
  <% total.times do |index| %>
    <span class="tw:h-1 tw:flex-1 tw:rounded-full <%= (index < number) ? "tw:bg-purple-500" : "tw:bg-gray-300 tw:dark:bg-gray-700" %>"></span>
  <% end %>
</div>
