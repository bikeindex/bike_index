<%= render(UI::Alerts::Base::Component.new(kind: :error,
      header: "#{@bike.title_string} is deleted!")) do %>
  <%= render(UI::Time::Component.new(time: @bike.deleted_at, format: :localize_time_precise,
        preposition: true)) %>
<% end %>
