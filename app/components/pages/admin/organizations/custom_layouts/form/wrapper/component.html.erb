<%# turbo: false - the tabs lead to admin pages Pages::Admin::Headers::Tabs' legacy JS binds on %>

<%= render(Pages::Admin::Organizations::Tabs::Component.new(organization: @organization, active: :custom_layouts,
      display_dev_info: @display_dev_info,
      subtitle:, additional_link:, turbo: false)) %>

<%= render(layout_form) %>
