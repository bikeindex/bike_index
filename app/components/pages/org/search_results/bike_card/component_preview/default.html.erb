<ul class="tw:m-0 tw:list-none tw:p-4 <%= Pages::Org::SearchResults::BikeCard::Component::LIST_CLASSES[layout] %>">
  <% bikes.each do |bike| %>
    <%= render(Pages::Org::SearchResults::BikeCard::Component.new(bike:, organization:, search_all:, layout:)) %>
  <% end %>
</ul>
