<%= render opening_page %>

<%# Below the gray. mt-24 clears the flow shell's -mb-18 pull-up. %>
<div class="tw:mt-24 tw:mb-8 tw:pl-4">
  <p
    class="
      tw:mb-4 tw:text-lg tw:font-semibold tw:text-gray-900 tw:italic
      tw:dark:text-gray-100
    "
  >
    <%= link_to translation(".go_back_to_old_view"), new_organization_bike_path(organization_id: @organization.to_param, old_view: true), class: "twlink" %>
  </p>

  <%= form_with(url: switches_path, method: :get, data: {turbo: false}) do %>
    <%= hidden_field_tag :register_settings, true %>

    <%= render UI::Forms::Checkbox::Component.new(name: :single_page, checked: @single_page,
        label: translation(".single_page")) %>

    <%= render UI::Forms::Checkbox::Component.new(name: :separate_attestation, checked: @separate_attestation,
        class_name: "tw:mt-2", label: translation(".separate_attestation")) %>

    <%= render UI::Button::Component.new(text: translation(".update"), color: :secondary, size: :sm,
        type: "submit", html_class: "tw:mt-3") %>
  <% end %>
</div>
