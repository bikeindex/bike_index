<% items.each do |item| %>
  <% case item[:type] %>
  <% when :divider %>
    <li class="divider-nav-item"></li>
  <% when :disabled %>
    <li>
      <span class="<%= disabled_classes(item) %>"><%= item[:label] %></span>
    </li>
  <% when :link %>
    <li><%= link_tag(item) %></li>
  <% when :super_admin_link %>
    <li class="less-strong mt-4">
      <%= link_to item[:label], item[:path], class: "nav-link" %>
    </li>
  <% end %>
<% end %>
