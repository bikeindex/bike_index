<button
  type="button"
  data-controller="registration-show--share"
  data-registration-show--share-url-value="<%= @url %>"
  data-action="registration-show--share#share"
  class="tw:w-full <%= UI::Button::Component.build_classes(color: :outline, size: :md, html_class: "tw:justify-center") %>"
>
  <span data-registration-show--share-target="label"><%= @text %></span>
</button>
