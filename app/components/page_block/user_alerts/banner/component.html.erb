<%# container, so it lines up with the navbar above it and the page content below %>

<div class="container">
  <%= render UI::Alerts::Base::Component.new(header: @header) do %>
    <%= content %>
  <% end %>
</div>
