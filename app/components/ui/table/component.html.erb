<div class="tw:mb-4" data-controller="table-overflow">
  <table class="<%= table_classes %>">
    <thead class="tw:text-xs tw:text-gray-700 tw:bg-gray-50 dark:tw:bg-gray-700 dark:tw:text-gray-400">
      <tr>
        <% @columns.each do |col| %>
          <th class="tw:px-4 tw:py-3">
            <% if col.sortable.present? %>
              <%= render_sortable(col.sortable) %>
            <% else %>
              <%= col.label %>
            <% end %>
          </th>
        <% end %>
      </tr>
    </thead>
    <tbody>
      <% @records.each do |record| %>
        <% cache_if(@cache_key, [@cache_key, record]) do %>
          <tr class="tw:bg-white tw:border-b tw:border-gray-200 dark:tw:bg-gray-800 dark:tw:border-gray-700">
            <% @columns.each do |col| %>
              <td class="tw:px-4 tw:py-3">
                <%= capture { instance_exec(record, &col.block) } %>
              </td>
            <% end %>
          </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>
</div>
