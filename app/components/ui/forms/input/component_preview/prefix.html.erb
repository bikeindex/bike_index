<%= form_with(url: "#", builder: BikeIndexFormBuilder) do |f| %>
  <%= render(UI::Forms::Input::Component.new(form_builder: f, attribute: :name, prefix: "@", html_options: {placeholder: "something.gov"})) %>
<% end %>
