<%# The radios belong to a form elsewhere on the page, which is how the org search renders them %>
<%= form_with(url: "#", method: :get, id: "period_preview_form") do %>
<% end %>

<%= render(component) %>
