{
  "name": "bike_index-webapp",
  "devDependencies": {
    "@herb-tools/formatter": "^0.8.2",
    "standard": "*"
  },
  "scripts": {
    "html:lint": "herb-format",
    "html:lint_check": "herb-format --check",
    "js:lint": "standard --fix",
    "js:lint_check": "standard --fix",
    "lint": "npm run js:lint & npm run html:lint",
    "lint_check": "npm run js:lint_check & npm run html:lint_check & wait"
  },
  "standard": {
    "ignore": [
      "/app/assets/",
      "/public/"
    ]
  }
}
